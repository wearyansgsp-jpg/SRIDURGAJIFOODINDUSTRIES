// Lovable error reporting removed — not needed for Cloudflare Pages deployment
export function reportLovableError(_error: unknown, _context: Record<string, unknown> = {}) {
  // no-op: was used for Lovable dev environment only
}
